# Boss Battle Prototype

Use Cocos Creator 3.8.8.

1. Open `cocosProject/Fight`.
2. Create a 2D scene if one is not already present.
3. Attach `BossBattle` to the scene Canvas/root node.
4. Preview.

Resources are loaded from `assets/resources` and the Boss Spine atlas is kept in its original source format.
